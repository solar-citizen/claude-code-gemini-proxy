{
  "name": "claude-code-gemini-proxy",
  "version": "0.3.0",
  "private": true,
  "description": "Local Anthropic Messages API <-> Gemini generateContent proxy for Claude Code Router",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "start": "bun run src/main.ts",
    "test": "jest",
    "typecheck": "typescript-native/bin/tsc --noEmit",
    "update:bun": "bun run bin/update-bun.js",
    "bump": "bunx npm-check-updates -u --workspaces --root && bun install && bun run update:bun"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/jest": "^30.0.0",
    "@types/node": "^26.1.1",
    "jest": "^30.4.2",
    "jest-environment-node": "^30.4.1",
    "ts-jest": "^29.4.12",
    "typescript": "^7.0.2"
  }
}
